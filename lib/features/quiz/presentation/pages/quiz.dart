import 'package:flutter/material.dart';

class Quiz extends StatelessWidget {
  final String quizId;

  const Quiz({required this.quizId, super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('res'));
  }
}
